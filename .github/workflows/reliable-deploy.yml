name: 🚂 Reliable Railway Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.11'
  DJANGO_SETTINGS_MODULE: jaytipargal.settings

jobs:
  # ==========================================
  # JOB 1: Build & Test
  # ==========================================
  build-and-test:
    name: 🔨 Build & Test
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        
    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 🔍 Run Django Checks
      env:
        SECRET_KEY: test-secret-key-for-ci-only
        DEBUG: 'False'
        DATABASE_URL: sqlite:///db.sqlite3
      run: |
        python manage.py check
        
    - name: 📊 Collect Static Files
      env:
        SECRET_KEY: test-secret-key-for-ci-only
        DEBUG: 'False'
        DATABASE_URL: sqlite:///db.sqlite3
      run: |
        python manage.py collectstatic --noinput
        
    - name: 📊 Generate Build Report
      run: |
        echo "✅ Build completed successfully"
        echo "📅 Build time: $(date)"
        echo "🔢 Commit: ${{ github.sha }}"

  # ==========================================
  # JOB 2: Deploy to Railway
  # ==========================================
  deploy:
    name: 🚀 Deploy to Railway
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v4
      
    - name: Check Railway Token
      id: check_token
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        if [ -n "$RAILWAY_TOKEN" ]; then
          echo "has_token=true" >> $GITHUB_OUTPUT
          echo "✅ Railway token found"
        else
          echo "has_token=false" >> $GITHUB_OUTPUT
          echo "⚠️ Railway token not found"
        fi
      
    - name: 🚂 Install Railway CLI
      if: steps.check_token.outputs.has_token == 'true'
      run: npm install -g @railway/cli
      
    - name: 🚀 Deploy to Railway
      if: steps.check_token.outputs.has_token == 'true'
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        echo "🚀 Starting Railway deployment..."
        railway up --service jayti
        echo "✅ Deployment triggered"
        
    - name: ⏳ Wait for Deployment
      if: steps.check_token.outputs.has_token == 'true'
      run: |
        echo "⏳ Waiting for deployment to stabilize..."
        sleep 30
        
    - name: 🔍 Verify Deployment
      if: steps.check_token.outputs.has_token == 'true'
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        echo "🔍 Checking deployment status..."
        railway status || echo "⚠️ Could not verify status"
        
    - name: 📊 Deployment Report
      if: always()
      run: |
        echo "=========================================="
        echo "📊 DEPLOYMENT REPORT"
        echo "=========================================="
        echo "🕐 Time: $(date)"
        echo "🔀 Branch: ${{ github.ref }}"
        echo "🔢 Commit: ${{ github.sha }}"
        echo "👤 Author: ${{ github.actor }}"
        echo "=========================================="

  # ==========================================
  # JOB 3: Post-Deploy Verification
  # ==========================================
  verify:
    name: ✅ Post-Deploy Verification
    needs: deploy
    runs-on: ubuntu-latest
    if: always() && needs.deploy.result == 'success'
    
    steps:
    - name: ⏳ Wait for Startup
      run: sleep 60
      
    - name: 🔍 Health Check
      run: |
        echo "🔍 Checking health endpoint..."
        echo "⚠️ Manual verification required after first deploy"
        echo "📝 Check: https://your-railway-url.up.railway.app/health/"
        
    - name: 🎉 Success Notification
      run: |
        echo "🎉 DEPLOYMENT SUCCESSFUL!"
        echo ""
        echo "🌐 Website: https://www.jaytibirthday.in"
        echo "🔑 Login: jayati / jayati2026"
